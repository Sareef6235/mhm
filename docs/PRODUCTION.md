# Production Notes

- Force HTTPS in cPanel.
- Move `database/` outside the web root after import where possible.
- Replace CDN assets with local copies if the host blocks external resources.
- Configure backups, upload size, SEO metadata, Google Analytics/Search Console, OTP, email and SMS from settings rows/pages.
- Run regular database backups and review `activity_logs`.
